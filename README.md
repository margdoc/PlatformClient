# Platform Client

## Running Client

Enter
```
yarn start
```
to run client server. It will be running on localhost on port 8080.

## Requirements

You must have nodejs packages listed in package.json. To install it enter
```
yarn install
```
Client server requires running Server. Now it has to be run on localhost:8000.